import * as Styled from './styles';

const Header = () => {
  return (
    <Styled.HeaderContainer>
      <Styled.HeaderTitle>{'< Júlio Nunes Tavares />'}</Styled.HeaderTitle>
    </Styled.HeaderContainer>
  );
};

export default Header;
